; Make SURE E_NOTICE is NOT included in the error_reporting flags, otherwise
; ANY undefined index will produce a warning (even though, in most cases, we're
; using '===' to handle undefined values).  ; 
error_reporting = E_ALL & ~E_DEPRECATED & ~E_NOTICE
